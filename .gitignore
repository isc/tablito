# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
CLAUDE.local.md

# Claude Code runtime state (per-clone). Skills/commands/agents partagés
# vivent aussi dans .claude/ — on ignore seulement les artefacts runtime.
.claude/*.lock
.claude/settings.local.json

# Skills installées via `npx skills add` (HyperFrames & co) : tooling local
# vendoré dans .agents/, symlinké dans .claude/skills/. Hors périmètre app.
.agents/
.claude/skills/
skills-lock.json

# Playwright tooling artifacts
.playwright-mcp/
.playwright-cli/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
